apiVersion: v1
kind: Service
metadata:
  name: {{ include "astro-agent.fullname" . }}
  labels:
    {{- include "astro-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "astro-agent.selectorLabels" . | nindent 4 }}
